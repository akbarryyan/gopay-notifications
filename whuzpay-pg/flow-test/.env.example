# Copy to .env and adjust if your local setup differs from defaults.

BASE_URL=http://localhost:8080

ADMIN_EMAIL=admin@pg-aggregator.local
ADMIN_PASSWORD=Admin123!

# Seeded demo merchant (back/seeds/001_initial_data.sql)
MERCHANT_ID=11111111-1111-1111-1111-111111111111

# Needed for ./run_all.sh production (webhook signature). By default this
# is read automatically from back/.env — only set it here if you want to
# override that (e.g. running flow-test against a remote/staging backend).
# CASHI_SECRET_KEY=
